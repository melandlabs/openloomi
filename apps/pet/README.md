# 🦊 OpenLoomi Pet

A desktop pet for OpenLoomi: the official Loomi fox sits on your desktop, watches what your local OpenLoomi app is **really doing**, and reacts with expressions — working, thinking, replying, waiting for your approval, sleeping — plus short speech bubbles generated by your own LLM key (DeepSeek-compatible).

<p align="center"><img src="assets/loomi/loomi-working.png" width="96" alt="working"> <img src="assets/loomi/loomi-thinking.png" width="96" alt="thinking"> <img src="assets/loomi/loomi-waiting.png" width="96" alt="waiting"> <img src="assets/loomi/loomi-happy.png" width="96" alt="happy"> <img src="assets/loomi/loomi-sleeping.png" width="96" alt="sleeping"></p>

## How it senses OpenLoomi (all local, all read-only)

| Source | Path / port | Drives |
|---|---|---|
| Process / local API | `openloomi.app` process, `127.0.0.1:3414/3415` (auto-detected) | awake / asleep / greeting on launch |
| Audit stream | `~/.openloomi/logs/audit.jsonl` | working expression + tool icon overhead |
| Main log | `~/.openloomi/logs/openloomi.log` | `[ERROR]` → error face; `Permission request/decision` → waiting (approval) / needs-input, auto-released on decision |
| Message store | `~/.openloomi/data/data.db` (sqlite, read-only) | new prompt → thinking; new reply → talking + speech bubble |

Nothing is written to OpenLoomi's data, and nothing leaves your machine except the optional bubble-generation call to your configured LLM provider.

## Run

**With the OpenLoomi client (default):** the pet starts automatically together with the app. Toggle it in **Settings → General → Desktop Pet** (enabled by default; the switch applies immediately). The preference lives in `~/.openloomi/pet-settings.json`; auto-launch looks for the pet at `apps/pet` (monorepo runs) or `OPENLOOMI_PET_PATH`.

**Standalone:**

```bash
cd apps/pet
npm install
npm start        # start the pet
npm run demo     # cycle through all 14 expressions (no real data)
```

Only one instance runs at a time (single-instance lock + pid file at `~/.openloomipet/pet.pid`).

## Speech bubbles (optional, BYOK)

Put your key in `~/.openloomipet/config.json`:

```json
{ "deepseekApiKey": "sk-..." }
```

or set `DEEPSEEK_API_KEY`. Without a key the pet still works — bubbles fall back to built-in lines. Tunables: `bubbleEveryMs` (min interval, default 90000), `muted`.

## Interactions

- **Drag** to move (position is remembered) · **click** to dismiss bubble / summon OpenLoomi · **right-click** for menu (summon / mute / log / quit) · tray icon has the same menu.

## States × artwork

14 expressions cut from the official Loomi sheet (`assets/loomi-src/loomi-sheet-green.png`, mapping in `assets/loomi/grid-map.json`):

`idle · roam · working · thinking · talking · juggling · sweeping · waiting · needsinput · happy · greet · attention · sleeping · error`

## Layout

```
main.js              Electron main process (window / tray / IPC)
preload.js           contextBridge API
renderer/            pet rendering (pet.html/css/js)
backend/
  watcher.js         OpenLoomi activity watcher (4 read-only sources + state machine)
  brain.js           speech bubble generation (rate-limited, offline fallback)
  config.js log.js   config & log (~/.openloomipet/)
assets/loomi/        14 state PNGs (transparent)
```
